services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: blog_pipeline
    ports:
      - "${POSTGRES_PORT:-5436}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
