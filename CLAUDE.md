# CLAUDE.md — 블로그 콘텐츠 자동화 파이프라인

## 1. 프로젝트 개요

- **목적**: 키워드/주제 입력 또는 PDF·엑셀 파일 업로드를 통해 트렌드 조사(키워드 입력 시) → 초안 생성 → SEO 메타데이터/이미지 alt 생성까지 자동화하고, 사람이 검토·수정 후 승인한 결과를 화면에 출력한다.
- **발행은 자동화하지 않는다.** 승인된 결과물을 사람이 직접 복사해 블로그 플랫폼(네이버 블로그)에 수동 업로드한다. 이 툴은 "콘텐츠 스테이징 툴"이지 발행 봇이 아니다.
- **단일 사용자용 내부 툴**이다. 멀티 테넌시, 권한 관리, 공개 배포는 고려하지 않는다.

## 2. 콘텐츠 카테고리

**메인 카테고리 (정보형 트랙)**
- 생활정보/제도안내 (정부지원금, 청약 공고, 세금·보험료 등 공공데이터 기반)
  - 서브타입: "입주 준비/체크리스트" — 청약 당첨·계약 이후 단계를 다루는 후속 포스트. 독자가 실제 구매 의도를 갖는 시점이라 쇼핑 커넥트 리빙상품 링크를 붙이기에 적합. 신청 단계를 다루는 본편(자격/일정 안내)에는 상품 링크를 넣지 않는다 — 검색 의도와 맞지 않아 신뢰도를 깎는다.
- 소프트웨어/서비스 스펙 비교 (제품 비교, 순위형 글)
- 선정 이유: 공개 자료 기반으로 사실 확인이 가능해 자동화 파이프라인과 궁합이 좋음. 개인 경험 서사가 필수가 아니라 E-E-A-T 문제에서 비교적 자유로움.

**보조 카테고리 (경험형 트랙, 비정기)**
- 여행 — 실제 다녀온 경험 기반. 정보형과 입력 방식이 반대(키워드가 아니라 사용자 경험 메모가 입력)이므로 별도 트랙으로 취급.

**제외한 카테고리**
- 맛집: 소개글에서도 뺀 항목 — 정식 카테고리 아님
- 보험/법률/금융 딥다이브: YMYL + 고경쟁, 초보 진입 부적합
- 순수 엔터프라이즈 기술 딥다이브(IAM/SCIM/OIDC 등): 시장이 좁아 트래픽 확보 어려움. 이런 주제는 별도의 "기술 블로그"(포트폴리오 목적)로 분리, 이 프로젝트 범위 아님
- 뉴스/시사성 단발 콘텐츠: 트래픽 반짝 상승 후 급락 — 단, 청약 공고처럼 반복 발행 가능한 템플릿형 콘텐츠는 예외로 검토 중 (미확정)

> [자리표시] 카테고리별 구체적인 서브토픽 목록, 콘텐츠 발행 주기는 추후 확정.

## 3. 발행 플랫폼 및 수익화

- **발행 플랫폼: 네이버 블로그.** 티스토리에서 전환 결정 — 생활정보 카테고리는 네이버 검색 의존도가 압도적으로 높고, 네이버는 외부 링크(쿠팡 등)가 많은 글의 노출·블로그 지수를 낮추는 경향이 있어 애초에 네이버 자체 생태계 안에서 도는 구조가 유리하다고 판단.
- **수익화 조합: 네이버 애드포스트 + 네이버 쇼핑 커넥트**
  - 애드포스트: 네이버가 문맥 기반으로 광고를 자동 배치, CPC/CPM 혼합 과금. 광고 위치는 직접 제어 불가 — 트래픽과 콘텐츠 주제가 사실상 유일한 레버.
  - 쇼핑 커넥트: 스마트스토어 제휴 프로그램. 쿠팡파트너스와 달리 네이버 자체 생태계라 블로그 노출에 불리하지 않음. 단, **API/자동화 미지원 — 링크 삽입은 반드시 사람이 네이버 블로그 에디터에서 수동으로 진행**(자동화 시도 시 스팸으로 간주되어 계정 제재 위험). 실적 인정 범위도 좁아서(해당 제품/스토어 판매만 인정) 아무 카테고리에나 붙이면 안 되고, 위 "입주 준비" 같이 구매 의도가 있는 콘텐츠에만 선택적으로 적용.
  - 애드포스트와 쇼핑 커넥트는 완전히 별개 서비스라 동시 운영 가능.
- **애드센스는 포기.** 네이버 블로그는 외부 광고 스크립트를 삽입할 수 없어 애드센스와 병행 불가. RPM이 애드센스보다 낮다는 게 업계 통설이라 절대 수익은 낮을 수 있으나, 검증 안 된 가정이므로 실측 후 재검토.
- **중복 콘텐츠 회피**: 발행 플랫폼을 네이버 단일로 고정했으므로 티스토리와의 중복 게시 이슈 자체가 발생하지 않음.

> [자리표시] 실측 데이터(트래픽, 애드포스트/쇼핑커넥트 수익) 확보 후 플랫폼 재검토 여지 있음. 데이터 모델은 계속 플랫폼 종속 없이 설계할 것.

## 4. 아키텍처

```
/repo
  CLAUDE.md              # 리포 루트에 유지 — Claude Code 자동 로딩 대상이라 /docs로 옮기지 않음
  docker-compose.yml     # 현재는 db(postgres)만. api/web은 배포처 확정 전까지 go run/npm run dev로 로컬 실행
  /apps
    /api                 # Go — 파이프라인 로직, 외부 API 연동, DB 접근. 구현 세부는 apps/api/CLAUDE.md
    /web                 # React (Vite) — 검토/승인 UI. 구현 세부는 apps/web/CLAUDE.md
```

- **Go API 서버**: REST API. 파이프라인 각 단계(조사/생성/메타데이터)를 순차 실행하고 상태를 기록. 외부 API 호출(트렌드/LLM/이미지)은 goroutine으로 병렬화 가능한 부분만 병렬화. 모듈/패키지 구조, DB 스키마 세부는 `apps/api/CLAUDE.md` 참고.
- **React 프론트**: 순수 클라이언트. 목록 화면(진행 중인 포스트 상태 확인) + 상세 화면(초안 보기/수정/승인·반려 버튼). SSR/SEO 불필요, Next.js 아님. 디렉토리 구조는 `apps/web/CLAUDE.md` 참고.
- **DB**: PostgreSQL 사용 확정. 스키마 세부는 `apps/api/CLAUDE.md` 참고.

## 5. 파이프라인 상태 머신 (도메인 규칙 — 스키마와 무관하게 유지)

```
[입력 방식 분기]
keyword_input               → researching  (트렌드/키워드 조사 중)
file_input (PDF/엑셀 업로드) ─┐
                             ├→ researched
keyword_input(researching 완료)┘
   → drafting            (초안 생성 중)
   → draft_ready
   → pending_review      (사람 검토 대기)
   → approved            (승인 완료 — 최종 화면 출력)
   → archived            (사용자가 수동 업로드 완료 표시)

pending_review → needs_revision → drafting   (반려 시 재생성 루프)
```

- **입력 방식 두 가지**: ① 키워드 입력 → `researching` 단계에서 트렌드/키워드 조사 자동 수행. ② PDF/엑셀 파일 업로드 → 이미 조사 자료가 주어진 상태이므로 `researching` 단계를 건너뛰고 바로 `researched`로 진입. 이후 흐름(drafting~archived)은 입력 방식과 무관하게 동일.
- 파일 업로드는 PDF 텍스트 추출 + 엑셀 파싱이 필요 — Go 쪽 파서 라이브러리 선정 등 구현 세부는 `apps/api/CLAUDE.md` 참고.

- 상태 전이는 Go API에서만 발생시킨다. 프론트는 상태를 읽고 액션(승인/반려)만 트리거한다.
- 각 단계 실패 시 `failed_<step>` 상태로 기록하고 에러 메시지를 남긴다. 재시도는 수동 트리거.
- 이 상태 목록과 반려 루프는 스키마 설계 시 반드시 반영해야 할 요구사항이다.
- 콘텐츠 트랙 구분: `content_type: informational | experiential` — 정보형/경험형 트랙에 따라 입력 단계(researching vs 사용자 경험 입력)가 달라짐.
- 쇼핑 커넥트 링크 삽입은 이 상태 머신 밖의 수동 후속 작업이다(archived 이후, 네이버 에디터에서 직접). 파이프라인이 관리하는 범위가 아님.

## 6. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 백엔드 | Go | 상태 머신·외부 API 병렬 호출에 적합, 기존 숙련도 활용 |
| 프론트 | React (Vite) | Next.js 불필요 — 내부 툴이라 SSR 이점 없음 |
| DB | PostgreSQL | 스키마 상세는 `apps/api/CLAUDE.md` |
| 트렌드 조사 | 네이버 데이터랩 API (1차) | 구글 트렌드는 공식 API 없음. 상세는 `apps/api/CLAUDE.md` |
| 초안 생성 | Anthropic Claude API | 공식 Go SDK 사용. 상세는 `apps/api/CLAUDE.md` |
| 배포 | > [자리표시] | Go: Fly.io/Railway 등, React: Vercel/Netlify 등 후보 |

## 7. 개발 규칙

### 개발 워크플로우
1. **설계/기획 단계는 Plan Mode로 진행한다.** 요구사항 정리, 구조 설계, 영향 범위 파악은 코드를 건드리기 전에 Plan Mode에서 끝낸다.
2. **코드 작업(파일 생성/수정, 명령 실행)은 반드시 사용자 승인을 거친다.** Auto-accept edits, bypass permissions 등 승인 절차를 건너뛰는 모드는 이 프로젝트에서 사용하지 않는다.
3. Plan Mode를 벗어난 뒤에도 계획에 없던 큰 변경(구조 변경, 새 의존성 추가 등)이 필요하면 먼저 알리고 승인받은 뒤 진행한다.

### 자동화 안전장치
- **발행/게시 API 연동 코드를 만들지 않는다.** 네이버 블로그에 글을 자동으로 올리는 기능은 이 프로젝트의 범위 밖이며, 실수로라도 그런 코드 경로를 추가하지 않는다.
- **쇼핑 커넥트 링크 삽입을 자동화하지 않는다.** 네이버 정책상 자동 삽입은 스팸으로 간주되어 계정 제재 위험이 있으므로, 링크 삽입은 항상 사람이 네이버 에디터에서 수동으로 진행한다는 원칙을 코드/UI 설계 전반에서 지킨다.
- 파이프라인의 최종 산출물은 항상 사람의 승인(`approved`)을 거친 뒤에만 화면에 노출하고, 승인 이전 단계 콘텐츠가 실수로 외부에 노출/전송되지 않도록 한다.

### 시크릿 관리
- 네이버 데이터랩 API 키, LLM API 키 등 민감정보는 `.env` 파일에 두고 `.gitignore`에 반드시 포함한다.
- `.env.example`로 필요한 키 목록만 (값 없이) 공유한다.
- 시크릿 값을 로그, 커밋 메시지, 코드에 직접 하드코딩하지 않는다.

### 코딩 스타일
- Go: 표준 `gofmt`/`golint` 규칙을 따르고, 에러는 반드시 반환값으로 처리하며 panic으로 흐름을 제어하지 않는다.
- React 관련 세부 컨벤션은 `apps/web/CLAUDE.md` 참고.
- 주석은 "왜"가 비직관적일 때만 남기고, 코드가 스스로 설명 가능한 부분(자명한 "무엇")에는 달지 않는다.

### Git 컨벤션
- 커밋 메시지는 Conventional Commits 형식을 따른다 (`feat:`, `fix:`, `refactor:`, `docs:` 등).
- `main` 브랜치에 직접 커밋하지 않고 feature 브랜치에서 작업 후 병합한다.
- 브랜치명은 커밋 타입 규칙과 맞춰 `<type>/<짧은-설명>` 형식을 쓴다 (예: `feat/pipeline-state-machine`, `fix/draft-retry`, `docs/architecture`).
- 브랜치 하나는 하나의 논리적 작업 단위(리뷰 가능한 크기)로 유지한다. 작업 범위가 커지면 더 작은 단위로 쪼갠다.
- 파이프라인 상태 머신, 외부 API 연동 등 핵심 로직 변경은 병합 전 반드시 리뷰를 거친다.
- **git commit, push는 반드시 사용자 승인 후에 실행한다.** 커밋/푸시 전 어떤 변경을 커밋하는지, 어디로 푸시하는지 먼저 알리고 승인받는다. 한 번의 승인이 이후 커밋/푸시를 모두 허용하는 것은 아니며, 매 커밋/푸시마다 다시 확인받는다.
- 커밋/푸시 승인을 요청하기 전에, 이번 작업에서 무엇을 했는지와 테스트 결과(통과 여부)를 대화창에 요약해서 먼저 보고한다.
- 사용법/실행 방법/설정값이 바뀌는 변경(새 패키지 추가, 새 env 변수, 새 API 엔드포인트 등)에는 해당 앱의 CLAUDE.md를 함께 업데이트한다.

### 테스트 규칙
- 파이프라인 상태 전이(특히 반려 루프 `pending_review → needs_revision → drafting`) 로직은 테스트 코드 작성을 필수로 한다.
- 외부 API(트렌드, LLM, 파일 파싱) 연동은 실패/타임아웃 시나리오를 반드시 테스트한다.
- 순수 스캐폴딩/설정 변경 등 실제 로직이 없는 작업은 테스트 예외로 한다.
- 관련 로직이 포함된 브랜치는 병합 전 로컬에서 테스트를 실행해 통과를 확인한다 (api: `go test ./...`, web: 테스트 프레임워크 도입 후 해당 명령).

### 로깅 / 모니터링
- 각 파이프라인 단계의 시작/완료/실패는 감사 추적이 가능하도록 로그를 남긴다 (`failed_<step>` 상태와 에러 메시지 포함).
- API 키 등 민감정보는 로그에 남기지 않는다.

### 의존성 관리
- 새 외부 라이브러리 추가 시 사전 승인을 받는다.
- 특히 PDF/엑셀 파서, LLM SDK 등은 검증된 라이브러리만 사용하고, 관리가 안 되는(unmaintained) 서드파티 패키지는 피한다.

### 환경 분리
- local(개발) / production(개인 운영) 환경을 분리하고, 각각 별도의 DB와 `.env`를 사용한다.
- 단일 사용자 내부 툴이므로 staging 환경은 필수는 아니며, 필요시에만 추가한다.

## 8. 로컬 개발

```bash
# 1. DB 기동
docker compose up -d db

# 2. 마이그레이션 적용 (golang-migrate CLI 필요: go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest)
migrate -path apps/api/migrations -database "$DATABASE_URL" up

# 3. API 서버 실행
cd apps/api && go run ./cmd/server

# 4. 프론트 개발 서버 실행
cd apps/web && npm install && npm run dev
```

각 앱의 `.env.example`을 복사해 `.env`를 만들고 값을 채운다 (`apps/api/.env.example`, `apps/web/.env.example`).
